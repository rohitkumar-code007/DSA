{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Debug bind",
            "request": "launch",
            "mainClass": "bind",
            "cwd": "${workspaceFolder}"
        }
    ]
}